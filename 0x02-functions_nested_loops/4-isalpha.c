#include "main.h"

/**
 * _isalpha - entry point
 * Description: Write a function that prints the alphabet, in lowercase.
 *Return: true value
 */
int _isalpha(int c)
{

if (c == 'c' || c == 'C')
{
return (1);
}
else
{
return (0);
}
}
