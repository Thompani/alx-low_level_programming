#include "main.h"

/**
 * _islower - entry point
 * Description: Write a function that prints the alphabet, in lowercase.
 *Return: true value
 */
int _islower(int c)
{

if (c >= 'c' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
